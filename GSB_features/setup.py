from setuptools import setup, find_packages
# pip install swifter
setup(name='GSB_features',
      description='Chromium feature extractor',
      version='0.1',
      packages=find_packages(),
      scripts=[])